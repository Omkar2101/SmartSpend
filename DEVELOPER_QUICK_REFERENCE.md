# SmartSpend Frontend - Developer Quick Reference\n\n## 🚀 Quick Start (Copy-Paste Ready)\n\n### Installation\n```bash\ncd frontend\nnpm install\n```\n\n### Environment Setup\nCreate `.env` file:\n```env\nVITE_CLERK_PUBLISHABLE_KEY=pk_test_your_key_here\nVITE_API_BASE_URL=http://localhost:5000/api/v1\n```\n\n### Start Development\n```bash\nnpm run dev\n```\n\n### Production Build\n```bash\nnpm run build\nnpm run preview\n```\n\n---\n\n## 📚 Common Tasks\n\n### Add a New Page\n\n1. **Create page component**:\n```typescript\n// src/pages/feature/FeaturePage.tsx\nimport React from 'react';\nimport './FeaturePage.css';\n\nexport const FeaturePage: React.FC = () => {\n  return (\n    <div className=\"feature-page\">\n      <h1>Feature Page</h1>\n    </div>\n  );\n};\n\nexport default FeaturePage;\n```\n\n2. **Add route**:\n```typescript\n// src/routes/routes.ts\nexport const ROUTES = {\n  // ...\n  FEATURE: '/feature',\n};\n```\n\n3. **Add to router**:\n```typescript\n// src/routes/AppRouter.tsx\nconst FeaturePage = lazy(() => import('../pages/feature/FeaturePage'));\n\n// In routes:\n<Route path={ROUTES.FEATURE} element={<FeaturePage />} />\n```\n\n### Add Navigation Link\n```typescript\n// src/components/layout/Sidebar.tsx\n<Link\n  to={ROUTES.FEATURE}\n  className={`nav-link ${isActive(ROUTES.FEATURE) ? 'active' : ''}`}\n>\n  🎯 Feature\n</Link>\n```\n\n### Fetch Data with Hooks\n```typescript\n// In component\nconst { data, isLoading, error } = useInvoices({\n  category: 'Transportation',\n}, { page: 1, limit: 10 });\n\nif (isLoading) return <LoadingSpinner />;\nif (error) return <Alert type=\"error\" message=\"Error loading\" />;\n\nreturn (\n  <div>\n    {data?.map(invoice => (\n      <div key={invoice.id}>{invoice.vendor}</div>\n    ))}\n  </div>\n);\n```\n\n### Create New API Hook\n```typescript\n// src/hooks/useApi.ts\nexport const useMyFeature = () => {\n  const apiClient = useApiClient();\n  \n  return useQuery({\n    queryKey: ['myFeature'],\n    queryFn: () => apiClient.myModule.getFeature(),\n    staleTime: 5 * 60 * 1000,\n  });\n};\n```\n\n### Use Forms with Validation\n```typescript\nconst [formData, setFormData] = useState<MyData>({\n  field: '',\n});\n\nconst handleSubmit = async (e: React.FormEvent) => {\n  e.preventDefault();\n  try {\n    await myMutation.mutateAsync(formData);\n    setSuccessMessage('Success!');\n  } catch (err) {\n    setErrorMessage('Error occurred');\n  }\n};\n\nreturn (\n  <form onSubmit={handleSubmit}>\n    <input\n      value={formData.field}\n      onChange={(e) => setFormData({...formData, field: e.target.value})}\n    />\n    <Button type=\"submit\" isLoading={myMutation.isPending}>\n      Submit\n    </Button>\n  </form>\n);\n```\n\n---\n\n## 🎨 Component Examples\n\n### Button Component\n```typescript\n<Button variant=\"primary\" size=\"md\" onClick={handleClick}>\n  Click Me\n</Button>\n\n// Variants: primary, secondary, danger, success\n// Sizes: sm, md, lg\n```\n\n### Alert Component\n```typescript\n<Alert\n  type=\"success\"\n  message=\"Operation completed\"\n  onClose={() => setSuccess(false)}\n/>\n\n// Types: success, error, info, warning\n```\n\n### Loading Spinner\n```typescript\n<LoadingSpinner text=\"Loading data...\" />\n```\n\n---\n\n## 🔌 API Integration Examples\n\n### Get Current User\n```typescript\nconst { data: user } = useCurrentUser();\nconsole.log(user?.email);\n```\n\n### Create Invoice\n```typescript\nconst createInvoice = useCreateInvoice();\n\nconst handleCreate = async () => {\n  await createInvoice.mutateAsync({\n    vendor: 'Uber',\n    totalAmount: 150,\n    category: 'Transportation',\n  });\n};\n```\n\n### Update Invoice\n```typescript\nconst updateInvoice = useUpdateInvoice();\n\nconst handleUpdate = async () => {\n  await updateInvoice.mutateAsync({\n    invoiceId: 'id-123',\n    data: { category: 'Food' },\n  });\n};\n```\n\n### Delete Invoice\n```typescript\nconst deleteInvoice = useDeleteInvoice();\n\nconst handleDelete = async () => {\n  await deleteInvoice.mutateAsync('id-123');\n};\n```\n\n### Connect Gmail\n```typescript\nconst connectGmail = useConnectGmail();\nconst { data: connection } = useGmailConnection();\n\nif (!connection) {\n  return <Button onClick={() => connectGmail.mutate()}>Connect Gmail</Button>;\n}\n```\n\n---\n\n## 📁 File Organization\n\n### Where to Add Different Things\n\n| What | Where |\n|------|-------|\n| New page | `src/pages/{feature}/` |\n| New component | `src/components/{category}/` |\n| New hook | `src/hooks/useApi.ts` |\n| New API client | `src/api/{module}.api.ts` |\n| New type | `src/types/index.ts` |\n| Styles | `src/{location}/*.css` |\n| Routes | `src/routes/routes.ts` |\n\n---\n\n## 🔍 Debugging Tips\n\n### Check React Query\n```typescript\nimport { QueryClient, useQuery } from '@tanstack/react-query';\n\n// Component\nconst queryClient = new QueryClient();\nconsole.log(queryClient.getQueryData(['invoices'])); // Check cache\n```\n\n### Log API Calls\n```typescript\n// In api/config.ts - already configured with logging\nclient.interceptors.request.use(config => {\n  console.log('Request:', config);\n  return config;\n});\n```\n\n### Check Clerk Auth\n```typescript\nconst { isSignedIn, getToken } = useAuth();\n\nconsole.log('Signed in:', isSignedIn);\ngetToken().then(token => console.log('Token:', token));\n```\n\n### Browser DevTools\n- React DevTools: Inspect components and props\n- Network tab: Check API requests\n- Console: Check logs and errors\n- Application tab: Check localStorage (Clerk)\n\n---\n\n## 🚨 Common Issues & Solutions\n\n### API Connection Error\n```\nError: ECONNREFUSED\n```\n**Solution**: Ensure backend is running on `http://localhost:5000`\n\n### CORS Error\n```\nAccess to XMLHttpRequest blocked by CORS\n```\n**Solution**: Check backend CORS configuration\n\n### Clerk Auth Error\n```\nClerk key not found\n```\n**Solution**: Add `VITE_CLERK_PUBLISHABLE_KEY` to `.env`\n\n### Type Error\n```\nType 'X' is not assignable to type 'Y'\n```\n**Solution**: Check `src/types/index.ts` for correct interfaces\n\n---\n\n## ⚡ Performance Tips\n\n1. **Use React Query for caching**:\n   ```typescript\n   // Good - cached and reused\n   const { data } = useInvoices();\n   \n   // Avoid - new request every time\n   const [data, setData] = useState();\n   useEffect(() => { fetchData(); }, []);\n   ```\n\n2. **Lazy load pages**:\n   ```typescript\n   // Already implemented in AppRouter.tsx\n   const Page = lazy(() => import('../pages/Page'));\n   ```\n\n3. **Minimize re-renders**:\n   ```typescript\n   // Use useCallback for event handlers\n   const handleClick = useCallback(() => { /* ... */ }, [dep]);\n   ```\n\n---\n\n## 📦 Dependencies\n\n- **react@19**: UI library\n- **react-router-dom@7**: Routing\n- **@tanstack/react-query@5**: Server state\n- **axios@1.16**: HTTP client\n- **@clerk/clerk-react@5**: Authentication\n- **typescript@6**: Type safety\n- **vite@8**: Build tool\n\n---\n\n## 🔗 Useful Commands\n\n```bash\n# Development\nnpm run dev          # Start dev server\nnpm run build        # Build for production\nnpm run preview      # Preview production build\nnpm run lint         # Run ESLint\n\n# Cleanup\nrm -rf node_modules && npm install  # Clean install\nnpm cache clean --force              # Clear npm cache\n```\n\n---\n\n## 📖 File Locations for Common Tasks\n\n### To add new endpoint:\n1. Backend: Add route in `backend/src/modules/*/routes.ts`\n2. Frontend types: Add type in `src/types/index.ts`\n3. API client: Add method in `src/api/{module}.api.ts`\n4. Factory: Register in `src/api/factory.ts`\n5. Hook: Add hook in `src/hooks/useApi.ts`\n\n### To add new page:\n1. Create: `src/pages/{feature}/{FeaturePage}.tsx`\n2. Create: `src/pages/{feature}/{FeaturePage}.css`\n3. Add route: `src/routes/routes.ts`\n4. Add to router: `src/routes/AppRouter.tsx`\n5. Add link: `src/components/layout/Sidebar.tsx`\n\n---\n\n## 🎯 Type Safety Checklist\n\n- [ ] All functions have return types\n- [ ] All props are typed with interfaces\n- [ ] No `any` types used\n- [ ] API responses match backend types\n- [ ] Form data matches API request types\n- [ ] Component props are exported interfaces\n\n---\n\n## 📝 Comments Template\n\n```typescript\n/**\n * Component/Hook Description\n * What it does, when to use it, any important notes\n */\n\nexport const Component: React.FC<Props> = ({ prop1, prop2 }) => {\n  // Implementation\n};\n```\n\n---\n\n## 🚢 Deployment Checklist\n\n- [ ] `.env` production variables set\n- [ ] Backend URL points to production\n- [ ] Clerk production keys configured\n- [ ] `npm run build` runs without errors\n- [ ] Build outputs to `dist/` folder\n- [ ] Deploy `dist/` to hosting\n- [ ] Test all features after deployment\n- [ ] Check console for errors\n- [ ] Verify API calls to production backend\n\n---\n\n## 📞 When Stuck\n\n1. Check documentation:\n   - [FRONTEND_SETUP.md](./FRONTEND_SETUP.md)\n   - [INTEGRATION_GUIDE.md](./INTEGRATION_GUIDE.md)\n\n2. Check component code:\n   - Look at similar components\n   - Follow existing patterns\n\n3. Check error message:\n   - Read error carefully\n   - Check browser console\n   - Check network tab\n\n4. Check types:\n   - Verify types match backend\n   - Check `src/types/index.ts`\n   - Use TypeScript strict mode\n\n5. Check API:\n   - Verify endpoint exists on backend\n   - Check request format\n   - Check response format\n\n---\n\n**Last Updated**: June 10, 2026\n